{{!--
  Generic confirm-dialog body (renderChatCard). `body` is a PRE-LOCALIZED string (may carry light <b>
  emphasis) → triple-stash so emphasis renders. Used by the shop's Yes/No confirms.
--}}
<p>{{{body}}}</p>
