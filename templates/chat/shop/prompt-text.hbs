{{!--
  Single-line text-prompt dialog body (renderChatCard from promptText). `initial` is the pre-filled
  value (auto-escaped by the double-stash); the dialog callback reads it by name="t".
--}}
<div class="form-group"><input type="text" name="t" value="{{initial}}"/></div>
